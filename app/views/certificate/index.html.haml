%h2 Certificate list
%table
  %tr
    %th Subject
    %th Issuer
    %th Valid until
  - @certificates.each do |certificate|
    %tr
      %td
        - if certificate.ca  
          CA: 
        = link_to certificate.CN, certificate
      %td= certificate.issuer
      %td= certificate.not_after
